import { Box, Typography } from '@mui/material';

// import { useState } from 'react';

// import { gifs, renderGifs } from '../utils/home';

const home = () => {
  //   console.log(renderGifs(gifs));

  return (
    <Box>
      <Typography>Hola soy home</Typography>
    </Box>
  );
};

export default home;
